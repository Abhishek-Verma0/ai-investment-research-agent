# core/rubric (M6)

The persona-weighted decision rubric used by the `decide` node.

Personas (`growth` / `value` / `balanced`) re-weight how signals (growth,
valuation, financial health, sentiment, risk) map to the final verdict and
conviction. Prompts live here, not inlined in node logic.
